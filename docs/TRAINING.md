# Training Guide

This project uses DistilBERT for mental health detection.

## Steps to train

1. Edit settings in `configs/train_config.yaml`
2. Run training:
3. The model checkpoint is saved to:

## Notes
- This training pipeline is designed for small synthetic datasets.
- For full datasets, update batch size, learning rate, and epochs.
